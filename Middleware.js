/**
 * Middleware:
 * Application level middleware:
 * Body parser is applicaiton level middleware that PIPE data in JSON
 * Cookie parser is also an application level middleware.
 *
 * Route level middleware
 * app.use(LoggerMiddleware);
 */